import styled from "@emotion/styled";

const Styled = styled.input`
  height: 38px;
  padding: 0 15px;
  border-radius: 4px;
  border: 1px solid #ddd;

  &.big {
    height: 40px;
  }

  &.small {
    height: 28px;
  }
`;

export default Styled;
